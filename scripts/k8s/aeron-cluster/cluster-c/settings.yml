---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aeron-cluster
spec:
  template:
    spec:
      containers:
        - name: aeronmd-c
          image: "< Ref to benchmark container >"
        - name: aeron-cluster-node
          image: "< Ref to benchmark container >"

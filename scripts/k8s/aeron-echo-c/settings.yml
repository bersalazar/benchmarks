---
# Update tolerations and nodeselectors
apiVersion: v1
kind: Pod
metadata:
  name: all
  # Additional multus network
  annotations:
    k8s.v1.cni.cncf.io/networks: aeron-network
spec:
  # Use Host networking
  # dnsPolicy: ClusterFirstWithHostNet
  # hostNetwork: true
  initContainers:
    - name: aeron-bootstrap
      image: "< Ref to bootstrap container >"

  containers:
    - name: aeronmd-c
      # This is the benchmarks container with an alternate entrypoint
      image: "< Ref to benchmark container >"
